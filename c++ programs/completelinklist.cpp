//Wap to choose a linked list operation
#include<iostream>
#include<stdio.h>
template<class t>
using namespace std;
typedef struct sllnode
{
	int data;
	sll *link;
}sll;
typedef struct dllnode
{
	int data;
	dll * next, * prev;
}dll;
t create(t *root)
{
}
t insertbeg(t *root)
{
}
t insertend (t *root)
{
}
t deletenode(t *root)
{
}
t deldata(t *root)
{
}
void print(t *root)
{
}
int main()
{ int choose, repeat, i,a;
  cout<<"choose type of linked list\n";
	cout<<"press 1 for singly and 2 for doubly 3 for circular singly 4 for circular doubly3\n"; 
 return(0);
}

